<div class="container mt-3">
  <%= link_to 'Back', edit_capsule_path(@capsule) %>
  <%= simple_form_for([@capsule, @message]) do |f| %>
    <%= f.input :title %>
    <%= f.input :content %>
    <%= f.button :submit %>
  <% end %>
</div>
